FROM tomcat:9.0-jdk17-openjdk

# Supprimer l'application par défaut
RUN rm -rf /usr/local/tomcat/webapps/*

# Copier votre fichier WAR
COPY target/ServiceExpress.war /usr/local/tomcat/webapps/ROOT.war

# Exposer le port
EXPOSE 8080

# Démarrer Tomcat
CMD ["catalina.sh", "run"]